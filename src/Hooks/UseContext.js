import { createContext } from "react";

export const AuthContext = createContext({});
export const UpdateContext = createContext({});
export const isMasterContext = createContext({});
export const isDarkContext = createContext({});